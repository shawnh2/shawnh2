~ uname -a

shawn, current a postgrad student @hdu


~ journalctl -uf -S "2023-01-01 00:00:00" -p 2 events

Feb 13 shanghai: starting an intership @qiniu


~ ps -hefL | grep 'focus'

cloud-native 001 api-gateway
cloud-native 002 network-proxy


~ cd /open-source-projects && dir -l |
  awk '{ if($1=="lang" && $2=="name" && $3=="status") printf "%-10s%-33s%-s10\n", $0}' |
  sed -e 's/active/*/' -e 's/inactive/ /'

golang    envoyproxy/gateway             *
golang    apache/dubbo-go-pixiu          *
golang    nutsdb/nutsdb
python    open-mmlab/mmcv
